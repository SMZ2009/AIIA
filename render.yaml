services:
  - type: web
    name: aiia
    env: node
    buildCommand: npm install && npm run build
    startCommand: npx prisma db push && npx tsx prisma/seed.ts && npm start
    envVars:
      - key: DATABASE_URL
        value: file:./prisma/dev.db
      - key: SESSION_SECRET
        generateValue: true
