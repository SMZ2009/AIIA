# 本地开发（PostgreSQL）
# DATABASE_URL="postgresql://postgres:postgres@localhost:5432/aiia"

# 生产环境（Render 自动注入，无需手动设置）
# DATABASE_URL — Render PostgreSQL 自动提供

# 会话密钥（生产环境 Render 自动生成，本地可随意填写）
SESSION_SECRET="dev-secret-change-in-production"
